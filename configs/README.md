# Configuration

Runtime and training CLI flags remain the source of defaults in this layout-only change.
Supply accepted calibration and electrical-profile JSONs by path; no replacement
production calibration or model is generated here. Dependencies remain in
`v4/requirements.txt` for existing Docker build contexts.
