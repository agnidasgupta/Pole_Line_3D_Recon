# V4-only clean layout

This branch uses the `v4` branch as its only production-content source. The directory design is new; no content was copied from `harpreet/v4-clean-root`.

The accepted implementation remains under `v4/` and `ops/v4*` until an audited compatibility migration is completed. All V4 experiments are catalogued under `experiments/`. V6.2 is intentionally absent.

See [repository layout](docs/REPOSITORY_LAYOUT.md) and [experiment index](docs/EXPERIMENTS.md).
