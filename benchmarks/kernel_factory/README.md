# Kernel Factory experiments

Place versioned definitions, workloads and exact checkers here. Generated candidates
and traces belong in `artifacts/`. The running performance campaign remains in
the separate `Pole_Line_3D_Recon_perf` checkout; this branch changes layout only.
