# V4 clean layout

This branch is a history-preserving child of `v4`, not an orphan copy. Future V4 production updates can be merged here normally.

See [layout policy](docs/REPOSITORY_LAYOUT.md) and [experiment registry](experiments/REGISTRY.tsv).
